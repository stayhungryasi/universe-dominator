# CLAUDE.md — UNIVERTRIX (우주지배자, univertrix.com)

## 프로젝트 정체
- 글로벌 시가총액 추적·시각화 사이트. 세계관: 기업=천체, 시총 1위=태양(왕좌).
- **존재 이유: 운영자(선장님)의 AI 시대 훈련장.** 기능 제안 기준은 "화려한가"가 아니라 "선장님을 강하게 만드는가".
- 완전 자동화가 북극성 — 수동 단계는 결함이다.
- 호칭: 운영자를 **선장님**으로 부른다.

## 스택 & 구조
- 정적 사이트: `scripts/build_site.py`가 `scripts/*-template.html` → 루트 `*.html` 12페이지 생성
- 데이터: `data/latest.json`(시총 스냅샷), `data/signals.json`(신호 관측소), `data/columns.json` 등
- 파이프라인: GitHub Actions `daily-update.yml` — quick(빌드+배포 ~2분) / full(전체 수집 ~10분), 러너 **python 3.12 고정**
- 3D: Three.js r128 (observatory-template.html의 boot3d), 텍스처 `assets/textures/`
- 호스팅: Cloudflare Pages

## 디자인 확정 스펙 (오로라, 2026-08 확정 — 임의 변경 금지)
- 낮: 스노우 #FBFBFC / 그래파이트 #16181D / 카드 #F5F5F7
- 밤: 우주 블랙 #0A0B0F / #F4F5F8 / 카드 #12141B
- 액센트: 인디고 #565CE8 → 사이언 #3EC8D8 (밤: #8B93FF → #4FE0EF)
- 서체: Pretendard 단일(위계는 굵기 100↔900), 로고 한글만 Nanum Brush Script
- 판형: **1080px 단일 기둥** — 헤더·메뉴·본문·히어로 오버레이 전부 동일 기둥
- 로고: 오로라 UNIVERTRIX(굵기 호흡 6s) + U→X 궤도선 + 붓글씨 우주지배자(광 스윕 호흡)
- 팔레트는 CSS 토큰 리맵 방식: `AURORA_GLOBAL_CSS`(build_site.py)가 전 페이지 </head> 직전 주입

## 작업 규칙 (반드시)
1. 코드 전달·커밋 전 검증: `python -m py_compile scripts/*.py` + 산출 HTML의 `<script>` 블록 `node --check`
2. **실빌드가 최고의 검증**: `python scripts/build_site.py` 로컬 실행 후 산출물 검사
3. `python scripts/selftest.py` 통과 필수 (수집 스크립트 전수 py_compile 포함)
4. 멱등성: 모든 주입은 마커 검사 후 1회만 (`ud-aurora-global-v1`, `ud-hdr-refine-v4`, `wide-fix` 등)
5. 자동 커밋 파일(columns.json 등)은 항상 원격 최신본 기준 — pull 먼저
6. 사고 시: 원인 규명 → 재현 검증 케이스 → 수리 → 재발 방지 3중화
7. 검증 불가 영역(첫 실전 실행 등)은 정직하게 리스크 고지
8. **기계 필드명은 화면에 노출 금지** (`eps_adj_ttm`·`coupon10y`·`guard`·원장 키 등) — 데이터·로그·페이지 소스는 대상이 아니다. 화면 문구는 내부 문자열을 번역하지 말고 **상태에서 생성**할 것: 매핑은 새 상태가 생기는 날 그대로 샌다 (2026-08-31 레전드 보드 비고)

## 알려진 지뢰 (재발 방지 기록)
- **f-string 중첩 따옴표**: 러너 3.11에서 SyntaxError났던 이력 → 3.12 고정했지만 변수 분리 습관 유지
- **3D 블록 부분 추출 금지**: observatory의 boot3d는 FLAG_COLOR·flagColor·sunTex·milkyTex·cleanupSolar와 한 몸 — 연속 범위로만 다룰 것 (2회 사고)
- **node --check는 미정의 참조를 못 잡는다** — 자유 함수 호출 vs 정의 대조 스캔 병행
- ~~주입 페이지 목록이 5곳에 흩어져 있음~~ → **2026-08-15 해결**: `build_site.py`의 `ALL_PAGES` 상수 하나로 통합(5개 주입 함수 전부 이 상수만 읽음). 새 페이지 추가는 ① build_xxx() ② main() 호출 ③ ALL_PAGES 등재 3단계뿐. selftest가 매 실행마다 ⓐ 5개 함수의 ALL_PAGES 사용 ⓑ 지역 목록 부활 여부 ⓒ 루트 HTML↔목록 일치를 검사해 등재 누락 시 즉시 실패. 주입 제외 파일은 `UNMANAGED_PAGES`(현재 my-universe.html 리다이렉트 스텁)에 명시
- 국기 이모지는 윈도우 크롬에서 깨짐 → flagcdn 이미지 사용
- 신호 pub 날짜는 RFC822/ISO 혼재 — email.utils 파싱 필수
- **프로브가 침묵하면 환경보다 프로브 문법부터 의심** (2026-08-17, 며칠간 반복 오판): 헤드리스 크롬 검증 스크립트가 아무 출력도 안 낼 때 "CDN 지연·virtual-time·load 이벤트 미발화" 같은 환경 탓으로 결론내고 정적 검증으로 갈음했는데, 진짜 원인은 프로브 코드의 문법 오류였다. 파이썬으로 JS를 생성하면서 `'\n'`이 **실제 개행**으로 풀려 문자열 리터럴이 깨졌고, 스크립트 블록 전체가 실행되지 않았다. 증상이 지독한 이유: `<pre>`는 초기값 그대로 남고 `window.onerror`조차 안 걸려 **완전 무증상**이다.
  - 규칙 ① 파이썬이 생성하는 JS에는 개행 이스케이프를 쓰지 말고 `String.fromCharCode(10)`
  - 규칙 ② 프로브 첫 줄에 `el.textContent='script-started'`를 박아 실행 여부부터 분리 판정
  - 규칙 ③ 그래도 침묵하면 dump에서 해당 `<script>` 원문을 눈으로 확인 (개행이 섞였는지)
  - 규칙 ④ "환경 탓"으로 결론내기 전에 동일 환경에서 3줄짜리 최소 페이지가 도는지 먼저 확인
  - 대가: 빈 상태 렌더·CSS 계산값·3D 간섭 등 여러 건을 실측 대신 추론으로 보고했다. 도구가 조용히 죽으면 검증한 줄 알고 넘어간다 — 침묵은 통과가 아니다.
- **CI 신선 체크아웃에서 파일 mtime은 판정 근거가 못 된다** (2026-08-21, 정비 관제탑 설계 중 발각): Actions는 매 실행 fresh checkout이라 **모든 파일의 mtime이 체크아웃 시각**이다. "이 산출물이 며칠째 갱신 정체인가", "이 봇이 이번 회차에 돌았는가"를 mtime으로 재면 무엇이 죽어 있든 영원히 "방금 갱신됨"으로 보인다 — 경보가 절대 안 울리는 **침묵하는 감시자**가 된다. 로컬에서는 멀쩡히 동작해 더 위험하다(로컬 mtime은 진짜라서 테스트가 통과한다).
  - 규칙 ① 갱신 시각 판정은 **파일 내용 속 타임스탬프**로 — `generated_at`(calendar·gurus·research), `generated_label`(buffett), 항목별 날짜 필드(columns) 등. 없으면 만들어서라도 내용에 심는다
  - 규칙 ② "이번 회차에 실행됐는가"는 파일로 알 수 없다(사건 없는 회차는 아무것도 안 쓴다) → 워크플로가 `steps.<id>.outcome`을 env로 주입하는 것이 CI에서 유일하게 믿을 수 있는 실행 흔적
  - 규칙 ③ mtime 폴백을 남기더라도 **폴백을 탔다는 사실을 로그에 찍을 것**. 조용한 폴백은 검증한 줄 착각하게 만든다
  - 규칙 ④ 스텝 결과를 읽는 설계라면 `run: cmd || echo "warning"` 형태를 조심 — `||`가 실패를 삼켜 outcome이 **항상 success**가 된다. 경고는 남기되 `|| { echo ...; exit 1; }` + `continue-on-error: true` 로 실패를 실패로 보고할 것
  - 뿌리는 '프로브 침묵' 지뢰와 같다: **침묵은 통과가 아니다.**
- **방지선은 막으려던 것의 '정의'로 검사하라 — 표면 특징으로 검사하면 오탐이 정상을 막는다** (2026-08-22, 동행 에세이 2편 오차단): 스크랩을 막으려고 만든 인용 규율을 **따옴표 유무 + 공백 기준 단어 수**라는 표면 특징으로 구현했다. 결과: 한국어 강조 표기(`"검색 사망론"`)가 인용으로 잡히고, `len(q.split())`이 한국어에서는 어절을 세는 바람에 15어절짜리 짧은 한국어 문장이 "15단어 인용"으로 걸렸다. 첫 실전 회차에서 Alphabet·Anthropic 2편이 통째로 보류됐다.
  - 증상이 지독한 이유: 방지선이 **작동하는 것처럼 보인다**. 로그에는 "인용 규율 위반 — 발행 보류"가 정직하게 찍히므로 규율이 일하는 줄 알지, 정상 글을 막고 있는 줄 모른다. 수집이 죽어 후보가 0이었다면 영영 못 봤을 것이다.
  - 규칙 ① 방지선을 만들 때 "무엇을 막는가"를 한 문장으로 적고, **그 문장 그대로** 검사하라. 여기서는 "수집한 원문을 옮겨 적는 것" → 검사도 수집 텍스트와의 대조여야 했다.
  - 규칙 ② 표면 특징(따옴표·기호·길이·패턴)은 대리 지표일 뿐이다. 대리 지표로 검사할 거면 **정상 사례로 먼저 오탐을 재라**.
  - 규칙 ③ 언어 가정을 점검하라. 단어 수·문자 수·정규식은 영어를 전제하기 쉽다 — 한국어 본문에 그대로 적용하면 기준이 몇 배로 빡빡해진다.
  - 규칙 ④ 방지선에 걸렸을 때 **즉시 폐기하지 말고 사유를 주고 한 번 더 기회를 줘라**. 오탐이든 진짜든 재생성 1회가 손실을 줄인다.
- **주입 지점(`</head>`)은 본문에 한 번이라도 더 쓰면 안 된다 — 주석도 본문이다** (2026-08-23, 관측노트 CSS 절반 소실): `build_site.py`의 주입 함수 5종은 전부 `html.replace("</head>", CSS, 1)` 로 **첫 번째** 발생 지점을 바꾼다. CSS 주석에 설명을 쓰며 `</head>` 라는 문자열을 적었더니, 주입 블록이 그 주석 한복판에 끼어들어 `<style>` 안에 `<style>` 이 생겼다. 주석이 닫히지 못하고 그 뒤 CSS 전체가 파서에서 버려졌다.
  - 증상이 지독한 이유: **HTML 은 멀쩡하고, 중괄호 균형도 맞고, `node --check` 도 통과한다.** 페이지는 열리고 대부분 정상으로 보이는데 뒤쪽 규칙만 조용히 죽는다. grep 으로 규칙을 찾으면 "파일에 있으니 됐다"고 착각하게 된다 — 실제로 그렇게 두 번 헛짚었다.
  - 규칙 ① 템플릿 본문·주석에 `</head>`·`</style>`·`</script>` 같은 **종료 태그 문자열을 쓰지 말 것** (설명이 필요하면 "head 끝" 처럼 풀어 쓴다).
  - 규칙 ② CSS 가 적용되지 않으면 파일 grep 으로 끝내지 말고 `document.styleSheets` 에서 **실제로 파싱됐는지** 확인할 것. 규칙 수가 파일보다 적으면 앞쪽에서 파서가 죽은 것이다.
  - 규칙 ③ selftest 가 매 실행 검사한다: 템플릿의 `</head>` 정확히 1회 · 산출물 첫 `<style>` 안에 또 다른 `<style>` 없음.
  - 곁가지로 배운 것: 주입 블록은 head 끝에 **나중에** 들어가므로 같은 특이도면 주입 쪽이 이긴다. 페이지에서 공용 클래스를 덮어쓰려면 특이도를 한 단 올려야 한다(`#id` 등).

- **감시자가 '무엇을 재는 자인가'를 착각하면 정상을 장애로 신고한다 — 게다가 수단이 이미 있어도 배선하지 않으면 없는 것과 같다** (2026-08-30, DeepMind 오경보): sentinel 의 신호 판정이 `signals.json` 의 **오늘 새로 captured 된 건수**로 소스 생사를 쟀다. 그건 '소스가 살아있나'가 아니라 '오늘 새 글이 올라왔나'를 재는 자다. Google DeepMind 는 매 회차 HTTP 200 으로 5건을 정상 응답했는데 신규 발행이 이틀 없다는 이유로 7회차 연속 0건으로 집계돼 경보가 나갔다. OpenAI·Anthropic 도 streak 2로 같은 길을 가던 중이었다.
  - 증상이 지독한 이유: **경보가 정상 작동하는 것처럼 보인다.** 로그도 텔레그램도 "0건 7회 연속"이라고 정직하게 말한다 — 그 '0건'이 무엇의 0건인지 아무도 되묻지 않을 뿐이다. 오경보가 반복되면 진짜 경보까지 무시하게 되므로, 침묵하는 감시자만큼이나 **양치기 감시자**도 위험하다.
  - 더 아픈 대목: 구분 수단(`fetch_status.json`)은 **8일 전(2026-08-22)에 이미 만들어져 있었다.** `judge_feeds` 는 쓰고 있었는데 `judge_signals` 만 안 읽었다. 도구를 만든 것과 배선한 것은 다르다 — 미결을 닫을 때 **그 수단을 써야 할 지점을 전수로 훑었는지** 확인할 것.
  - 규칙 ① 감시 지표를 정할 때 "이 수치가 0이면 **무엇이** 잘못된 것인가"를 한 문장으로 적어라. 답이 "새 글이 없다"면 그건 장애 지표가 아니라 활동 지표다.
  - 규칙 ② 산출물(결과 개수)이 아니라 **요청 결과(outcome)** 로 생사를 판정하라. 산출물은 세상이 조용해도 0이 된다.
  - 규칙 ③ 한 사건에 두 곳이 울지 않게 관할을 명시하라. 요청 실패 = `judge_feeds` 단독, 0건 응답 = `judge_signals` 단독. 테스트는 "경보가 났다"가 아니라 **"정확히 1건 났다"** 로 검사한다.
  - 규칙 ④ 판정 자로를 바꾸면 **옛 자로의 눈금(state)을 이어받지 마라.** 오경보로 켜진 `alerted:True` 를 그대로 두면 다음 정상 회차에 '회복 ✅' 이 나간다 — 죽은 적 없는 소스에 회복을 알리는 규약 오용이다. `migrate_state`(v1→v2)가 눈금을 내리고, 이관 사실을 로그에 찍는다.
  - 규칙 ⑤ 판정부 단위 테스트가 전부 통과해도 **배선이 끊겨 있으면 실전만 틀린다.** `RunWiringTest` 가 `run()` 을 실제로 돌려 원장의 outcome 이 로그까지 흘러오는지 본다. 이 테스트는 배선을 끊는 변이(mutation)로 **실패하는 것까지 확인**했다 — 실패할 수 없는 테스트는 감시자가 아니다.

## 판단층 헌법 (2026-08-31 개정 — 기계가 채우고 사람이 덮어쓴다)
- 판단층은 두 겹이다: `data/buffett_auto.json`(기계) + `data/buffett_config.json`의 `buffett` 블록(사람 **오버라이드 전용**)
- **자동값은 기계가 매일 갱신 가능** — `fetch_buffett_auto.py`가 full 마다 다시 쓴다
- **사람 값은 기계가 수정 금지** — 어떤 스크립트도 `buffett_config.json`을 쓰지 않는다(읽기만)
- **화면은 origin을 표시한다** — 자동값에는 "자동" 라벨. 자동값에 사람의 권위를 씌우지 않는다
- 병합은 **필드 단위**(`scripts/buffett_layers.py` 한 곳에만): 사람 값이 null 아니면 사람, null이면 자동, 둘 다 없으면 null. 블록 통째로 고르면 "한 칸 취재했더니 나머지가 비는" 역설이 생긴다
- `{"value": null}` 자리표시자는 **없는 값**으로 친다. 값으로 치면 기계가 잰 숫자를 영원히 덮는다

## 미결 (등재만 — 지시 없이 착수 금지)
- ~~**신호 소스별 fetch 상태 기록 → 정비 관제탑 2단계**~~ (등재 2026-08-21) → **2026-08-22 해결**: `scripts/feed_client.py` 공용 클라이언트가 소스별 결과를 `data/fetch_status.json`에 남긴다(`ok`/`zero`/`http_error`/`timeout` + 코드 + 건수). sentinel의 `judge_feeds`가 이를 읽어 **요청 실패는 2회 연속에 경보**하고 **0건은 관대한 임계(4회) 유지** — "조용한 날"과 "죽은 소스"가 갈렸다. 동행 7개 소스도 같은 원장에 실려 사각지대가 닫혔다.
  - 0건 임계를 4로 남긴 것은 선장님 결정이다(2026-08-21). 구분 수단이 생겼다고 자동으로 낮추지 않는다 — 낮출지는 운영 데이터를 보고 판단한다.

## 텔레그램 채널 분리 (2026-09-01 확정)
- 공개 `@stayhungryasi` = **관측 결과만** (모닝브리핑·왕좌 교체·동행·필독 공지)
- 정비 관제 경보·회복 알림·자동 취재 갱신 = **`TELEGRAM_ALERT_CHAT_ID`(소장 DM) 전용**
- **미등록이면 공개로 폴백하지 않는다 — 로그로만 남긴다.** 폴백 방향이 반대면 "설정을 안 하면 새는" 구조가 된다(실제로 '응답 회복 ✅' 가 공개 채널로 나갔다). 안전한 기본값은 침묵이다
- 워크플로에서 운영 스텝에는 `TELEGRAM_CHAT_ID` 를 **아예 넘기지 않는다** — 변수가 거기 있으면 언젠가 폴백 경로가 되살아난다

## 커뮤니케이션
- 한국어, 간결하게. "허접한데?" 류 터프한 피드백 = 품질 요구 → 즉시 개선으로 응답
- 큰 변경 전 요약 보고, 파괴적 작업(삭제·강제 푸시)은 반드시 사전 확인
- 배포: push 후 Actions에서 Run workflow(quick) — 배포 확인까지가 작업 완료
